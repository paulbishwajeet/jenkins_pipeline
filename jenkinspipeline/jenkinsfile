@Library("pipeline-shared-lib")
import com.acceleratedskillup.*;

pipeline {

	agent none
	stages {
		stage('Build') {
			steps {
				script {
					def calculator = new Calculator()
					println canculator.add(1,2)
					
					}
				}
			}
			}

		}
